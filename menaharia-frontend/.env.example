VITE_API_URL=http://localhost:3002

# Public URL of this frontend (used for Chapa return links in docs / dev)
# Backend PAYMENT_REDIRECT_URL / FRONTEND_URL must be:
#   http://localhost:5173/payment-return.html
# (not the SPA route — payment-return.html calls POST /v1/payments/callback first)
VITE_APP_URL=http://localhost:5173